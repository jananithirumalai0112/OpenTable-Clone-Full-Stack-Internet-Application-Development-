namespace OpenTableApp.Models
{
    public enum PriceRange
    {
        Dollar = 1,
        DoubleDollar,
        TripleDollar,
        QuadrupleDollar
    }

    public enum UserRole
    {
        Admin,
        User
    }
}
